require 'spec_helper'

describe Scrollytelling::LoadingSpinner do
  it 'has a version number' do
    expect(Scrollytelling::LoadingSpinner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
